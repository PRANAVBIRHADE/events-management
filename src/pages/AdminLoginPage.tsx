import React from 'react';
import { Box, Heading, Text } from '@chakra-ui/react';

const AdminLoginPage: React.FC = () => (
  <Box p={8}>
    <Heading>Admin Login</Heading>
    <Text mt={4}>Admin login form will go here.</Text>
  </Box>
);

export default AdminLoginPage;
